<!--
councilify template: council supporting-root bundle.md.
Implemented behavior-first contract: this root composes Anchors and the
council's own primary behavior only.
Fill every {{PLACEHOLDER}}, then delete this instruction block.
-->
---
bundle:
  name: {{DOMAIN}}-council
  version: 0.1.0
  description: The {{DOMAIN_TITLE}} Council — {{LENS_COUNT}} orthogonal {{DOMAIN_TITLE}} lenses plus a /{{DOMAIN}}-council orchestrator that runs cold fan-out, debate-to-consensus, and synthesized verdict with recorded dissent.

includes:
  # Supporting root: compose Anchors and this bundle's primary behavior.
  - bundle: git+https://github.com/microsoft/amplifier-foundation@main#subdirectory=bundles/anchors/bundle.md
  - bundle: {{DOMAIN}}-council:behaviors/{{DOMAIN}}-council-behavior
# The local behavior reference omits .yaml; the resolver appends it.
---

@anchors:context/system.md
